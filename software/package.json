{
  "name": "assambl-software",
  "version": "0.1.0",
  "private": true,
  "description": "Assambl: modelado de residencias en woodframe. Orquestación de frontend y backend en desarrollo.",
  "scripts": {
    "setup": "npm --prefix frontend install && node scripts/setup_api.cjs",
    "dev": "concurrently -k -n api,web -c blue,magenta npm:dev:api npm:dev:web",
    "dev:web": "npm --prefix frontend run dev",
    "dev:api": "node scripts/dev_api.cjs",
    "check": "npm --prefix frontend run check && node scripts/dev_api.cjs --test"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
